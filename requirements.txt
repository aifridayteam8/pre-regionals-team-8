# Unified requirements for the merged backend (backend/) + parser stack.
# Loose >= pins matching the versions verified working in this environment.

# Flask core
Flask>=3.0
Flask-RESTX>=1.3
Flask-JWT-Extended>=4.6
Flask-CORS>=4.0
Flask-Migrate>=4.0
Flask-SQLAlchemy>=3.1

# Database
SQLAlchemy>=2.0

# Serialization / validation
marshmallow>=3.20
python-dotenv>=1.0

# AI / LLM (TCS GenAI Lab gateway, OpenAI-compatible; ollama kept for the local client)
langchain-openai>=0.3
openai>=1.7
httpx>=0.27
ollama>=0.1.7

# Testing
pytest>=7.4
